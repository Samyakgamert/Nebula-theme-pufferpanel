 # Nebula-inspired PufferPanel theme

This folder contains a simple PufferPanel theme package that uses a dark, glowing "Nebula" look.

## Files
- `manifest.json` — theme settings and configuration
- `theme.css` — custom styling

## Packaging
To use this theme in PufferPanel, create an uncompressed `.tar` archive containing these files at the root.
